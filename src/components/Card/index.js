import React from 'react'
import { CardContainer, CardContent, CardTitle, CardBody } from './CardElements'

const Card = () => {
    return (
        <CardContainer>
            <CardContent>
                <CardTitle></CardTitle>
                <CardBody></CardBody>
            </CardContent>
        </CardContainer>
    )
}

export default Card
